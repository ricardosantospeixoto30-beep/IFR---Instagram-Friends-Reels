<?xml version="1.0" encoding="utf-8"?>
<!--
    s50 — total opt-out of Android's backup/transfer mechanisms.

    Rationale from user (s49b): "quero que se desinstalar isto tudo
    relacionado a isto seja apagado para não ficar lixo a poluir o
    telemóvel". Uninstall already deletes /data/data/<package>
    automatically (Room DB, SharedPreferences, cache), but backup
    services could otherwise capture snapshots and restore them on
    the same device via install-if-restored. Excluding every domain
    below guarantees that no Friends Reels data ever leaves the
    device, and any future reinstall starts from an empty state.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
